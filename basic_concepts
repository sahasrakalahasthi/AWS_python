============= functions ===========
def add(x,y):
    c=x+y
    return c
a=int(input())
b=int(input())
res=add(a,b)
print(res)

 #include"math.h" ----for userdefined header file in c lang

=======importing methods========

import math
math.sqrt(100)
10.0
import math as m
m.sqrt(100)
10.0
from math import *
sqrt(100)
10.0

==============errors=============

with open("abc.txt") as f1:
    pass
==========Perfect number==========
(using for loop)
def perfect(a):
    sum=0
    for i in range(1,n):
        if(n%i==0):
            sum=sum+i
    if(sum==n):
        print("perfect")
        
n=int(input())
perfect(n)

(using while loop)

